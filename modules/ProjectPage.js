import React from 'react'

// holding page for user navigating to specific project page.
// accessed by /project/:id route path

export default React.createClass({
  render() {
    return (
        <div>
            <h2>Easy Tiger</h2>
            <p>This project aint ready for it's own page just yet</p>
        </div>
    )
  }
})